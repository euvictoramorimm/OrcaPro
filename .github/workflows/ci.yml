name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Testes do Backend
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      DIRECT_URL: ${{ secrets.DIRECT_URL }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      NODE_ENV: test

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: OrcaPro/backend/package-lock.json

      - name: Instalar dependências
        working-directory: OrcaPro/backend
        run: npm ci

      - name: Gerar Prisma Client
        working-directory: OrcaPro/backend
        run: npx prisma generate

      - name: Checar tipos TypeScript
        working-directory: OrcaPro/backend
        run: npx tsc --noEmit

      - name: Rodar testes
        working-directory: OrcaPro/backend
        run: npm test

  frontend:
    name: Frontend — tipos e build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: OrcaPro/frontend/package-lock.json

      - name: Instalar dependências
        working-directory: OrcaPro/frontend
        run: npm ci

      - name: Checar tipos TypeScript
        working-directory: OrcaPro/frontend
        run: npx tsc --noEmit

      - name: Build de produção
        working-directory: OrcaPro/frontend
        run: npm run build

  deploy:
    name: Deploy no Render
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Disparar deploy
        run: curl -s "${{ secrets.RENDER_DEPLOY_HOOK }}"
